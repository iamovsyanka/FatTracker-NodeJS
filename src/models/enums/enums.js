const meals = ['Breakfast', 'Lunch', 'Dinner', 'Snack'];
const roles = ['admin', 'user'];
const sex = ['men', 'women'];
const activity = [1.2, 1.37, 1.55, 1.7, 1.9];

module.exports = {
  meals,
  roles,
  sex,
  activity
};
